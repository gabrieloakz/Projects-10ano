// gabriel ferreira carvalho 10E

#include <iostream>
#include <locale>
#include <string> //biblioteca pra trabalhar com strings
using namespace std;

• Principais funções para manipulação de strings:

str1.compare(str2) comparação de strings

str.length() devolve o tamanho da string

str1.copy(str, tamanho, inicio) copia a string

str1.append(str2) concatenar as duas strings
 
toupper() coloca str em letras maiúsculas

tolower() coloca str em letras minúsculas

